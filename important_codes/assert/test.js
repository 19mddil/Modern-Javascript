describe("pow", function () {
    it("2 is raised by the power 3", function () {
        assert.equal(pow(2, 3), 8);

    });
    it("2 is raised by the power 3 again", function () {
        assert.equal(pow(2, 3), 18);

    });
});